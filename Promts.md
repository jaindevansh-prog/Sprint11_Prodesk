## PROMTS USED 

1. What is the most efficient way to upload image buffers from Express to Cloudinary using Multer memory storage without saving files temporarily to the local disk?

2. How can I handle optimistic UI updates or instant DOM mutations in React after triggering an asynchronous DELETE request to MongoDB via Axios?

3. How do I resolve Node.js `querySrv ECONNREFUSED` DNS lookup errors when connecting to a MongoDB Atlas cluster over local network environments?